import { Appbar } from "react-native-paper";

export const TopBar = ({ title, navigation, goBack = false }) => {
  return (
    <Appbar.Header style={{ backgroundColor: "#fff", height: 10, }}>
      
    </Appbar.Header>
  );

};
